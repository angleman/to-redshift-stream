// to-redshift-stream by angleman, MIT
// sort safe base16 to/from base64 conversion with filename and url safe characters written in pure javascript

function Module(config) {
	self = this
	API  = {
		option: function() {
		},
		option2: function() {
		}
	}
	return API
}

module.exports = Module()